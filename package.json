{
  "name": "@harms-haus/pi-powerline",
  "version": "1.0.0",
  "description": "pi-coding-agent extension: unified powerline footer and status bar",
  "keywords": [
    "pi-package"
  ],
  "files": [
    "src/**/*.ts",
    "!src/__tests__/**",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "main": "src/index.ts",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint:fix": "eslint --fix src/",
    "pre-commit-check": "npm run format:check && npm run lint && npm run typecheck",
    "prepare": "simple-git-hooks"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.6",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "simple-git-hooks": "^2.13.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.3",
    "vitest": "^4.1.6"
  },
  "simple-git-hooks": {
    "pre-commit": "npm run pre-commit-check"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "author": "harms-haus",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/harms-haus/pi-powerline.git"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
